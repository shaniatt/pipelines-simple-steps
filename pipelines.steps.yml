pipelines:
  - name: barebones_pipeline
    steps:
    - name: step_1
      type: Bash
      configuration:
      execution:
        onExecute:
          - printenv
 
    - name: step_2
      type: Bash
      configuration:
        inputSteps:
          - name: step_1
      execution:
        onExecute:
          - printenv
 
    - name: step_3
      type: Bash
      configuration:
        inputSteps:
          - name: step_1
      execution:
        onExecute:
          - printenv
